{% extends 'backend/master.html.twig' %}

{% block title %}{{ user.name }} - Alterar Password{% endblock %}
{% block contentTitle %}{{ user.name }} - Alterar Password{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">{{ user.name }} - Alterar Password</h6>
                </div>
                <div class="card-body">
                    {{ include('backend/user/_change_password_form.html.twig', {'button_label': 'Atualizar'}) }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
