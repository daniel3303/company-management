{# @var paginator \Knp\Component\Pager\Pagination\PaginationInterface #}
{% if paginator is not empty %}
    <div class="d-flex justify-content-center">
        {{ knp_pagination_render(paginator) }}
    </div>
{% endif %}