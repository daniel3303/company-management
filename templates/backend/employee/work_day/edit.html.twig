{% extends 'backend/master.html.twig' %}

{% block title %}Editar dia de trabalho {{ workDay.day|date("d-m-Y") }}{% endblock %}
{% block contentTitle %}Editar dia de trabalho: {{ workDay.day|date("d-m-Y") }}{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-12">
            <div class="card shadow mb-4">
                <div class="card-header py-3">
                    <h6 class="m-0 font-weight-bold text-primary">Editar dia de trabalho {{ workDay.day|date("d-m-Y") }}</h6>
                </div>
                <div class="card-body">
                    {{ include('backend/employee/work_day/_form.html.twig') }}
                    <hr/>
                    <a href="{{ path('backend_employee_employee_show', {'id': workDay.employee.id}) }}"
                       class="btn btn-secondary btn-icon-split">
                        <span class="icon text-white-50">
                          <i class="fas fa-arrow-left"></i>
                        </span>
                        <span class="text">Voltar à lista</span>
                    </a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
